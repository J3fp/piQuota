{
  "name": "pi-quota",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Read-only quota monitor for the AI provider credentials Pi already owns: terminal CLI, Pi TUI line, and Moshi usage and approval cards",
  "engines": {
    "node": ">=20.0.0"
  },
  "bin": {
    "piquota": "./bin/piquota.js"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "quota": "node bin/piquota.js",
    "quota:json": "node bin/piquota.js --json"
  },
  "license": "MIT"
}
